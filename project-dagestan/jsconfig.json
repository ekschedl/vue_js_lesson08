{
  "compilerOptions": {
    "target": "es5",
    "module": "esnext",
    "baseUrl": "./src", // Устанавливаем базовый путь для всех импортов
    "paths": {
      "@/*": ["*"] // Это позволяет использовать алиас @ для указания на папку src
    },
    "lib": ["esnext", "dom", "dom.iterable", "scripthost"]
  },
  "include": [
    "src/**/*" // Указываем, что все файлы в папке src должны быть доступны для компиляции
  ]
}
